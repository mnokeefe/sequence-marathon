// Drop the runners collection in Mongo and recreate it with data from the config file
//var activities = require('./tasks/create-runners');

// Update the loop
//var theloop = require('./tasks/theloop');

// Get activities
var activities = require('./tasks/activities');
